[package]
name = "heraclitus_runtime"
version = "1.0.0"
edition = "2021"

[dependencies]
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
pdf-extract = "0.7" # 🟢 Added native binary PDF layout parsing crate
