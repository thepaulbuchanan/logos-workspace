# 🧳 Heraclitus Platform: Session Handover Registry

## 1. Active Current State
The project directory is a fully functioning, warning-free, flattened repository architecture running natively on macOS. 

## 2. Active Structural Components Checklist
*   **Physics Engine (`core/`):** A 3-file compiled Rust binary pipeline managing LaTeX parsing, dynamic bootstrapping, and Lean 4 subprocess shell handshakes.
*   **Unified Ledger Library (`lemmas/`):** Contains human-machine unified spec documents for `SVE-L101`, `SVE-L102`, `SVE-L103`, `SVE-L201`, `SVE-L301`, `SVE-L402`, `SVE-L403`, `SVE-L501`, and `SVE-L601`.
*   **Visual Monitor Interface (`svi_ui.py`):** Real-time terminal progress bar gauge panel calculating active writing hygiene metrics.

## 3. Resume Commands For the Next Session
To pick up exactly where this session paused, run these commands inside your terminal:
```bash
# 1. Boot up the real-time monitoring playground dashboard instantly
python3 svi_ui.py

# 2. Run a clean manual background compilation target test check
cd core && cargo clean && cargo run
```
