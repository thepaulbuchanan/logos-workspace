#!/usr/bin/env bash
set -euo pipefail
echo "Bootstrap is now an explicit, opt-in step."
echo "This script only copies curated material; it never synthesises or overwrites."
# Add selective copy logic here later if needed
