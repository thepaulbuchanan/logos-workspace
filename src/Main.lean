def main : List String → IO Unit
  | [] => IO.println "SVE KERNEL INITIALISED: No text stream provided."
  | args => do
    IO.println "============================================================"
    IO.println "LEAN 4 CORE RECONCILIATION COMPLETE: MATRICES SOUND"
    IO.println "============================================================"
