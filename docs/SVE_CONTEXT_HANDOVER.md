# 📥 SVE Project Context Handover Protocol (Session-Dumping Safe)
**To the Invariant AI Core:** You are resuming an active development session for the **Semantic Verification Initiative (SVI)**. Do not output generic onboarding pleasantries. Absorb this state matrix and await the user's tactical execution command.

---

## 1. System Core State Ledger
*   **Active Project Repository:** The **Heraclitus Engine** (A neuro-symbolic linter that extracts the invariant logical *Logos* out of natural human text streams).
*   **Current Architecture Level:** Version 2.4.0 (Decoupled, Dynamic, Self-Bootstrapping, Path-Resilient).
*   **Active Local Code Files:** 
    *   `core/src/main.rs`: File text stream ingestion loop orchestration.
    *   `core/src/latex.rs`: Layout aware regex macro cleaning engine.
    *   `core/src/engine.rs`: Dynamic YAML front-matter compiler & Lean 4 subprocess process hook layer.
    *   `core/src/lemmas.rs`: Global negative logic boolean modifier mapping trackers.
    *   `svi_ui.py`: Real-time python automated dashboard tracking telemetry.

---

## 2. In-Flight Verification Constraints
The engine is currently tracking a 7-paragraph test manuscript vector inside `tests/manuscript.tex`. It successfully boots, dynamic-loads rules off disk, executes local `lean` background processes for formulas, and outputs a strict **14% Objectivity Metric** with exact paragraph sync:
*   Block 1: Verified Clean Baseline AST.
*   Block 2: Red Alert -> `SVE-L402_TIMELINE_OVERREACH` (Catches unhedged "will" over future horizons).
*   Block 3: Red Alert -> `SVE-L101_CIRCULAR_LOOP_ALERT` (Catches circular tracking parameters loops).
*   Block 4: Red Alert -> `SVE-L301_CONSENSUS_FALLACY` (Catches "experts agree" rhetorical substitutions).
*   Block 5: Verified Clean Baseline AST.
*   Block 6: Blue Alert -> `LEAN4_MATH_VERIFIED` (Natively evaluates `2 + 2 = 4` via your MacBook's Lean toolchain).
*   Block 7: Red Alert -> `SVE-L501_SINGLE_CAUSE_FALLACY` (Catches Causal Monism phrases like "entirely due to").

---

## 3. Strategic Project Trajectory
We have chosen to mirror the exact community scaling mechanics of Lean's **Mathlib** (Monorepo rule, Zulip async threading, atomic PR gating) translated into a narrative open-source framework named **`LogosLib`**. 

The code utilizes the **Unified Lemma Layout Standard**: an integrated single-file schema containing human-readable specs, First-Order Logic math, machine-readable `sve` symbolic logic arrays, and a secure `ep_hash` anchor.

---

## 4. Current Directive State
The workspace files are compiled clean, warning-free, and pushed to `main` via GitHub Desktop. The user is ready to select the next technical development module: **Automated Line/Coordinate Mapping Extraction** or **The In-Memory PDF Extractor Layer**.
